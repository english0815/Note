沒問題！以下為你整理 MCU 韌體工程師面試中**最核心、最常考的題目**，並直接附上**標準答案與技術要點**。這份資料依據核心基礎到實戰應用分類，建議在面試前熟讀。

## **一、 C 語言與記憶體管理 (必考魔王題)**

### **Q1：volatile 的作用是什麼？韌體開發中什麼時候「必須」使用它？**

* **答案：** volatile 告訴編譯器（Compiler）該變數的值隨時可能被外部硬體或非同步事件修改，因此**不要對其進行優化（Optimization）**。每當程式需要讀取或寫入該變數時，CPU 必須直接從記憶體（RAM）或硬體暫存器位址存取，而不是為了好快而讀取 CPU 快取（Cache）或暫存器中的備份。  
* **必須使用的三個場景：**  
  1. **硬體暫存器對映（Memory-mapped I/O registers）：** 如 GPIO 狀態暫存器，硬體狀態隨時在變。  
  2. **中斷服務程式（ISR）與主程式共享的全域變數：** 防止主程式將該變數優化進 CPU 暫存器而讀到舊值。  
  3. **多執行緒（Multi-threaded）或 RTOS 任務間共享的全域變數。**

### **Q2：static 用在全域變數、區域變數、函式前，分別代表什麼？**

* **答案：**  
  * **靜態區域變數（Static Local Variable）：** 延長變數的**生命週期**。該變數只會初始化一次，當函式結束時，變數不會消失，其值會保留到下一次函式呼叫。  
  * **靜態全域變數（Static Global Variable）：** 限制變數的**作用域（Scope）**。該變數只能在定義它的 .c 檔案內部被存取，其他檔案即使使用 extern 也無法存取，避免命名衝突。  
  * **靜態函式（Static Function）：** 同樣限制作用域。該函式只能在定義它的 .c 檔案內部被呼叫，屬於檔案隱私（封裝）。

### **Q3：請說明 MCU 的記憶體佈局（Memory Layout）中，BSS, Data, Heap, Stack 分別放什麼？**

* **答案：**  
  * **Text (Code) 段：** 存放唯讀的機器碼（程式指令）和常數（const 變數）。（存放在 Flash）  
  * **Data 段（Initialized Data Segment）：** 存放**已初始化**且初始值不為 0 的全域變數和靜態變數。（執行時從 Flash 搬到 RAM）  
  * **BSS 段（Block Started by Symbol）：** 存放**未初始化**或**初始值為 0** 的全域變數和靜態變數。（系統啟動時會自動清零，省 Flash 空間）  
  * **Stack（堆疊）：** 由編譯器自動分配與釋放。存放函式的**區域變數、函式參數、返回位址**。中斷發生時的上下文（Context）也會推入 Stack。  
  * **Heap（堆積）：** 供程式設計師動態分配（透過 malloc / free）的記憶體空間。

### **Q4：指標宣告辨析：const int \*p、int const \*p 與 int \* const p 的區別？**

* **答案：**  
  * const int \*p 與 int const \*p：兩者完全相同。**指標所指向的資料是常數**，不能透過 \*p \= 10; 修改資料，但指標本身可以指向別的位址（p \= \&x; 可行）。  
  * int \* const p：**指標本身是常數**。指標鎖定只能指向同一個記憶體位址，不能修改（p \= \&x; 錯誤），但可以修改該位址裡的資料（\*p \= 10; 可行）。

## **二、 微控制器 (MCU) 架構與週邊介面**

### **Q5：請比較 I2C, SPI, UART 三種常見通訊協定的差異。**

* **答案：**

| 特性 | UART | I2C | SPI |
| :---- | :---- | :---- | :---- |
| **同步/非同步** | 非同步 (無時脈線) | 同步 (有時脈線 SCL) | 同步 (有時脈線 SCK) |
| **訊號線路數** | 2 條 (TX, RX) | 2 條 (SDA, SCL) | 4 條 (MOSI, MISO, SCK, CS) |
| **傳輸方向** | 全雙工 (Full-Duplex) | 半雙工 (Half-Duplex) | 全雙工 (Full-Duplex) |
| **架構** | 點對點 (Peer-to-Peer) | 主從架構 (多主多從，定址制) | 主從架構 (一主多從，片選線 CS 制) |
| **速度** | 慢 (通常 \< 115.2 kbps) | 中 (100k \- 3.4 Mbps) | 快 (可達數十 Mbps) |
| **硬體特點** | 無 | 需要**上拉電阻** (Open-Drain) | 需要獨立的 CS 線控制不同裝置 |

### **Q6：寫中斷服務程式（ISR）時有哪些限制與注意事項？**

* **答案：**  
  1. **快跑快出：** ISR 必須極短且高效，絕對不能有 Blocking（如 delay 或是等待某個旗標）。  
  2. **不能有返回值，也不能帶參數。**  
  3. **不能呼叫耗時或不可重入的函式：** 例如 printf()，或是會觸發動態記憶體分配的 malloc() / free()。  
  4. **與主程式共享變數時：** 該變數必須加上 volatile 關鍵字保護。

### **Q7：什麼是 DMA？它與輪詢（Polling）、中斷（Interrupt）有何不同？**

* **答案：**  
  * **Polling (輪詢)：** CPU 一直死等硬體狀態改變（極度浪費 CPU 資源）。  
  * **Interrupt (中斷)：** 硬體準備好時通知 CPU，CPU 暫停手邊工作來搬移資料（頻繁中斷會造成系統 Context Switch 的開銷）。  
  * **DMA (Direct Memory Access)：** 允許週邊硬體直接與記憶體（RAM）交換資料，**完全不需要 CPU 介入**。資料搬移完成後，DMA 控制器才會發出一個中斷通知 CPU 來處理資料。  
  * *效益：* 大幅解放 CPU，讓 CPU 可以去專心處理複雜的演算法或進入低功耗睡眠。

## **三、 即時作業系統 (RTOS) 觀念**

### **Q8：Mutex（互斥鎖）與 Binary Semaphore（二進位信號量）有何不同？**

* **答案：**  
  雖然它們的數值都只有 0 與 1，但核心設計概念不同：  
  1. **所有權（Ownership）：** Mutex 有所有權的概念。哪個任務（Task）上鎖的，就必須由該任務解鎖。而 Semaphore 沒有所有權，任務 A 上鎖，任務 B 可以幫它解鎖（常用於同步）。  
  2. **優先權反轉機制：** Mutex 通常具備優先權繼承（Priority Inheritance）機制，用來防止優先權反轉；Semaphore 則沒有。

### **Q9：什麼是優先權反轉（Priority Inversion）？RTOS 如何解決？**

* **答案：**  
  * **現象：** 當一個低優先權任務（Task L）持有了某個共享資源（如 Mutex），一個高優先權任務（Task H）因為拿不到資源而進入等待（Blocked）。此時，一個中優先權任務（Task M）不需要該資源，卻因為優先權高於 Task L 而搶佔了 CPU 執行。這導致 Task L 遲遲無法結束並釋放資源，間接使得高優先權的 Task H 被中優先權的 Task M 卡住。  
  * **解決方案（優先權繼承）：** 當高優先權的 Task H 嘗試獲取已被 Task L 持有的 Mutex 時，系統會臨時將 Task L 的優先權**提升到與 Task H 相同**。這樣 Task L 就能壓制 Task M 並快速執行完畢、釋放資源。釋放後，Task L 的優先權恢復原狀，Task H 順利接手執行。

## **四、 實戰除錯與晶片底層 (硬實力測試)**

### **Q10：當系統當機並跳進 HardFault\_Handler 時，你該如何 debug？**

* **答案：**  
  1. **讀取核心暫存器：** 發生 HardFault 時，MCU 會自動將當前的上下文（Context）推入堆疊（Stack）。我會透過除錯器（如 GDB/IAR/Keil）檢視 **SP（堆疊指標）**，並找出 **PC（程式計數器）** 與 **LR（連結暫存器）** 的值。  
  2. **定位程式碼：** LR 指向的是發生異常前的下一條指令位址。對照編譯出來的 **.map 檔案** 或反組譯視窗（Disassembly），就能精準找到是哪一行程式碼引發了當機。  
  3. **分析原因：** 常見原因包括：  
     * 空指標（Null Pointer）或野指標（Wild Pointer）解引用。  
     * 陣列越界、記憶體位址未對齊存取（Unhandled Misaligned Access）。  
     * Stack Overflow（堆疊溢位）。

### **Q11：如何用位元操作（Bitwise）將暫存器 REG 的 Bit 3 設為 1、清零、以及反轉？**

* **答案：**  
  * **設為 1 (Set)：** REG |= (1 \<\< 3);  
  * **清零 (Clear)：** REG &= \~(1 \<\< 3);  
  * **反轉 (Toggle)：** REG ^= (1 \<\< 3);

### **Q12：什麼是 Race Condition（競態條件）？在韌體中如何保護「臨界區」（Critical Section）？**

* **答案：**  
  * **定義：** 當兩個或多個程序（例如主迴圈與中斷 ISR，或 RTOS 中的多個任務）同時存取並修改同一個共享變數時，最後的結果取決於它們執行的相對順序，導致不可預期的錯誤。  
  * **保護方法（臨界區保護）：**  
    1. **裸機（Bare-metal）環境：** 在存取共享變數前**關閉總中斷**（Disable Interrupts），存取完後再開啟中斷。  
    2. **RTOS 環境下：** 使用 **Mutex（互斥鎖）**、進入 **Critical Section（利用作業系統提供的 API 禁能排程或中斷）**，或者使用訊號量進行同步。
